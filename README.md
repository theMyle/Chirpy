# Chirpy

Learning Side Project - RESTful Backend Server using Golang.

## Tech Stack

- Go (`net/http`) for the HTTP server and routing
- `PostgreSQL` for database
- `goose` for schema migrations
- `sqlc` for query generation
